// Remember, we're gonna use strict mode in all scripts now!
"use strict";

let data = [17, 21, 23];

function printForecast(arr) {
  let str = "";

  for (let i = 0; i < arr.length; i++) {
    str = str + `${arr[i]}C in ${i + 1} `;
  }

  console.log(`${element}C in ${i} day`);
}
